# Active Tasks · 任务进度持久化

> 这是 LLM 跨会话任务**历史**的完整记录（大文件，按需读）。
> **默认不写**：日常 sk-* 的在途状态由 `.task-pulse` 承载；仅 Librarian / 需长期任务历史 / 用户显式要求时，才把任务详情写入本文件（见 CLAUDE.md §1.5、postamble.md、task-pulse.md）。
> 治"健忘"的机制已由 `.task-pulse`（心跳）+ `.checkpoint`（断点续跑）承担。

---

## 进行中的任务

_（空 — 新工作区无在途任务。日常在途状态由 `.task-pulse` 承载；仅 Librarian / 需长期任务历史 / 用户显式要求时才在此登记，见文末「何时才写本文件」。）_

---

## 已完成的任务（保留 30 天，之后归档）

_（空）_

---

## 待开始的任务（队列）

_（空）_

---

## 条目格式示例

> 下面整段在代码块内，仅示范字段结构，**不会被当成真实任务状态解析**。真实登记时照此结构、用真实标的替换占位代码。

```text
### task-001            ← 进行中示例
- **id**: task-001
- **created**: 2026-04-07
- **target**: 某算力芯片 (60XXXX.SH)
- **skill**: sk-company-deepdive
- **type**: deepdive
- **status**: in_progress
- **progress**: 已完成 §1-§3，卡在 §4 利润驱动，需补 2024 Q4 毛利率
- **blocker**: 缺 2024 Q4 实际毛利率
- **next_step**: 等 Q4 财报或先用 Q3 推演并标「待核验假设」
- **expected_output**: coverage/60XXXX_示例/deepdive/2026-04-07-deepdive.md
- **last_touched**: 2026-04-07 14:30

### task-099            ← 已完成示例
- **id**: task-099
- **target**: 某电池龙头 (30XXXX.SZ)
- **skill**: sk-thesis
- **status**: done
- **completed_at**: 2026-04-05 16:20
- **output**: coverage/30XXXX_示例/thesis/2026-04-05-thesis.md
- **outcome_summary**: 命题「产能出清 + 储能放量」，3 个必要条件已列

### queue-001            ← 队列示例
- **target**: 某 CPU 设计 (68XXXX.SH)
- **skill**: sk-company-deepdive
- **type**: new_coverage
- **priority**: high
- **trigger**: PM 询问 AI 算力国产替代第二选择
- **deadline**: 2026-04-12
```

---

## 字段说明

| 字段 | 含义 |
|---|---|
| **id** | 唯一 ID（task-NNN，简单递增） |
| **created** | 任务创建日期 |
| **target** | 标的（公司、行业、主题），含 ticker |
| **skill** | 用哪个 sk-* skill 执行 |
| **type** | 任务类型（new_coverage / update / preview / postmortem / thesis_challenge ...） |
| **status** | `in_progress` / `done` / `blocked` / `cancelled` |
| **progress** | 文字描述当前做到哪一步 |
| **blocker** | 当前卡在哪（"无"或具体说明） |
| **next_step** | 下次接着做什么 |
| **expected_output** | 预期归档路径（按 output-archive.md 规范） |
| **last_touched** | 最后一次更新时间 |
| **completed_at** | 完成时间（done 状态才有） |
| **outcome_summary** | 完成时的一句话总结（done 状态才有） |
| **priority** | 优先级（high / medium / low），仅 queue 有 |
| **trigger** | 任务触发原因（谁要求的、什么事件触发的） |
| **deadline** | 截止日期 |

---

## LLM 使用本文件的规则

> 前置：以下读写流程**仅在"何时才写本文件"（见文末）触发时**适用。日常任务默认只走 `.task-pulse` + `.checkpoint`，不执行下面的读写。

### 启动 skill 前（仅当本任务被纳入 active-tasks 历史时）

1. 读本文件
2. 找：是否存在 `target` 与本次任务相同的 in_progress 任务？
3. 如果有 → 读 `progress` + `next_step`，从断点继续
4. 如果无 → 创建一条新的 in_progress 任务，分配新 id，写入文件

### 完成 skill 后（随 postamble.md「Step 1 · 归档 + 更新任务状态」一并执行）

1. 找到本次对应的 task（按 id）
2. 如果完成全部任务：
   - status → done
   - completed_at → 当前时间
   - output → 实际归档路径
   - outcome_summary → 一句话总结
3. 如果只完成部分：
   - 更新 progress 字段
   - 更新 last_touched
   - 如果遇到 blocker，写入 blocker 字段
   - 写入 next_step
4. 如果产生了后续任务 → 添加到 queue 区

### 用户启动新会话时

新会话的在途任务提示由 `.task-pulse` 承担（见 CLAUDE.md §1.1 / task-pulse.md），**不读本文件**。仅当用户明确要任务历史详情时，才读 active-tasks.md。

---

## 维护原则

- in_progress 不超过 5 个（超过说明上下文太散，应该清理或合并）
- 完成的任务保留 30 天后归档到 `archive/active-tasks-{YYYY-MM}.md`
- queue 不超过 20 个（超过说明优先级管理失败）
- 每周复盘一次：检查是否有 in_progress > 7 天没动的"僵尸任务"，要么继续做要么 cancel

---

## 何时才写本文件

**默认不写**。日常 sk-* 的在途状态由 `.task-pulse` + `.checkpoint` 承载，不重复写 active-tasks.md。

**仅以下情况才把任务详情写入本文件**：

- Librarian 类交付需要长期任务历史
- 用户显式要求"记到任务历史里"
- 需要跨多会话追踪、且 `.task-pulse`（仅存在途状态）不足以承载的复杂项目
